PORT=5000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# AI Providers (Optional - will fall back to smart local decision engine if not provided)
GEMINI_API_KEY=
OPENAI_API_KEY=
DEFAULT_AI_PROVIDER=auto

# Web Search (Optional - built-in fallback supported)
ENABLE_WEB_SEARCH=true
